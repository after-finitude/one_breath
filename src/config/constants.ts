// UI Constraints
export const MAX_THOUGHT_LENGTH = 280;
export const ENTRIES_PER_PAGE = 10;
export const PREVIEW_LENGTH = 100;

// Toast notifications
export const DEFAULT_TOAST_DURATION = 3000;

// Export
export const EXPORT_FILE_PREFIX = "one-breath";

// Date formats
export const MONTH_KEY_LENGTH = 7; // YYYY-MM format

// Retry configuration
export const DEFAULT_MAX_ATTEMPTS = 3;
export const DEFAULT_INITIAL_DELAY = 100;
export const DEFAULT_MAX_DELAY = 2000;
export const DEFAULT_BACKOFF_FACTOR = 2;

// Storage
export const STORAGE_TEST_KEY = "__storage_test__";

// Languages
export type Language = "en" | "ru";
export const SUPPORTED_LANGUAGES: Language[] = ["en", "ru"];
export const DEFAULT_LANGUAGE: Language = "en";
